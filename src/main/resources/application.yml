server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/darbar_hotel_db
    username: root
    password: sisirbsb6060
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB
